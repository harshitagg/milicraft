<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div class="alert alert-error">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "form-control", :required => true %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text, :class => "form-control", :required => true, :style => "height: 500px;" %>
  </div>
  <div class="actions">
    <%= f.submit 'Post it', :class => "btn btn-lg btn-primary" %>
  </div>
<% end %>
