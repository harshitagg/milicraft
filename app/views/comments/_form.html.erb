<%= form_for([@post, @comment]) do |f| %>
    <p>
      <%= f.label :email %><br/>
      <%= f.email_field :email %>
    </p>
    <p>
      <%= f.label :body %><br/>
      <%= f.text_area :body %>
    </p>
    <p>
      <%= f.submit %>
    </p>
<% end %>